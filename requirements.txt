# Core oracle engine — no API key needed
streamlit>=1.35.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
scikit-learn>=1.3.0
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# Optional — only needed for Layer 3 auto-scoring via Claude LLM
# anthropic>=0.40.0
